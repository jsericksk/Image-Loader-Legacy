<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/menu_navigationview_bookmarks"
            android:icon="@drawable/ic_favorite"
            android:title="@string/label_bookmarks"/>
		<item
			android:id="@+id/menu_navigationview_feedback"
			android:icon="@drawable/ic_feedback"
			android:title="@string/menu_navigationview_feedback"/>
		<item
			android:id="@+id/menu_navigationview_share"
			android:icon="@drawable/ic_share"
			android:title="@string/menu_navigationview_share"/>
    </group>
</menu>
